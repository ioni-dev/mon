<h1>Forgot your password?</h1>

<%= form_for :driver, Routes.driver_reset_password_path(@conn, :create), fn f -> %>
  <%= label f, :email %>
  <%= email_input f, :email, required: true %>

  <div>
    <%= submit "Send instructions to reset password" %>
  </div>
<% end %>

<p>
  <%= link "Register", to: Routes.driver_registration_path(@conn, :new) %> |
  <%= link "Log in", to: Routes.driver_session_path(@conn, :new) %>
</p>
